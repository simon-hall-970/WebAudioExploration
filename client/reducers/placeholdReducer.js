const initialState = {}

function placeholderReducer (state = initialState, action) {
    switch (action.type) {
        //case 'ACTION_TYPE':
            //return {...state,
                //key: action.value}

        default:
            return state
    }
}

export default placeholderReducer